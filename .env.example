# Example environment variables (do NOT commit real secrets)
PINECONE_API_KEY=your-pinecone-key
PINECONE_INDEX=bajaj2
PINECONE_DIM=1536

# Google Gemini
GOOGLE_API_KEY=your-gemini-api-key
GEMINI_MODEL=gemini-1.5-pro

# Embeddings (Gemini)
EMBEDDING_MODEL=text-embedding-004
# Limit how many chunks to embed per request (helps avoid rate limits)
MAX_CHUNKS=20

# Auth for /api/v1/hackrx/run
TEAM_TOKEN=replace-with-team-token

# Optional: run Next.js on port 8000 to match docs
# PORT=8000
